import type { ShowHidePrimitive } from "@govtech-bb/form-types";

export const ShowHide: ShowHidePrimitive = {
  fieldId: "show-hide",
  htmlType: "show-hide",
  label: "Show / hide",
  // no validations — the toggle is not a required/validated field
};
